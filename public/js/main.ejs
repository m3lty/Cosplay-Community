<%
toCaps = function(string){
  return string.charAt(0).toUpperCase() + string.slice(1);
};
%>
<%
countLikes = function(obj){
  var likes = 0;
  for (var i = 0; i < obj.length; i++) {
    likes++;
  }
  return likes;
}
%>
